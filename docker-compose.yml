services:
  webhook:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./config/profiles.yaml:/config/profiles.yaml:ro
    restart: unless-stopped
